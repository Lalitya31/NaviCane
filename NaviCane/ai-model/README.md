# AI Model Placeholder

This folder is a placeholder for the on-device model and metadata. Replace with optimized ONNX or TFLite artifacts.

- yolov7-tiny/
  - model.onnx (add actual model)
  - labels.txt
  - config.json

Integration notes:
- Load model in browser via ONNX Runtime Web or run inference on edge device and send events to dashboard.
